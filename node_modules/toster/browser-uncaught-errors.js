export function installUncaughtErrorHandler(runner) {
  let originalHandler = window.onerror || function() {};
  window.onerror = function(msg, url, line, column, err) {
    runner._uncaughtErrorHandler(err);

    originalHandler.apply(this, arguments);
  };
}
