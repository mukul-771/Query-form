

const replacement = {
  './uncaught-errors': './browser-uncaught-errors.js'
};

export default {
	entry: 'index.js',
	plugins: [
    {
      resolveId(file) {
        if(replacement[file]) {
          return replacement[file];
        }
        return null;
      }
    }
  ]
};
